import React from 'react';

const Home = () => (
  <div>
    <h1>I am the very verty =Best home component.</h1>
    <button onClick={() => console.log('In there')}>Press Me!</button>
  </div>
);

export default Home;